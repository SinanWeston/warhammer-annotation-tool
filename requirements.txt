# Python dependencies for photoanalyzer.
# Install into the yolo_env venv:
#   yolo_env/bin/pip install -r requirements.txt

# ─── Core ML (backend inference + training) ───────────────────
ultralytics>=8.4           # YOLO training + inference
torch>=2.4                 # PyTorch (YOLO, DINO)
torchvision>=0.19          # vision transforms
transformers>=4.49         # SAM 3, SAM 2, OWLv2 visual-prompt, Grounding DINO
accelerate>=0.30           # transformers accelerator
huggingface-hub>=0.26      # gated-model auth for SAM 3 / SAM 2

# ─── Dataset augmentation ─────────────────────────────────────
albumentations>=1.4        # scripts/augment_dataset.py
numpy>=1.26
opencv-python>=4.9
Pillow>=10.0
PyYAML>=6.0
tqdm>=4.66
scipy>=1.13

# ─── Scraper suite (scripts/*.py) ─────────────────────────────
requests>=2.31
beautifulsoup4>=4.12
praw>=7.8                  # Reddit
yt-dlp>=2024.1             # YouTube
scenedetect[opencv]>=0.6.3 # YouTube frame extraction
flickrapi>=2.4             # Flickr
icrawler>=0.6              # Bing/Google fallback
python-dotenv>=1.0
cloudscraper>=1.2          # eBay anti-bot
playwright>=1.44           # scripts/scrape_ebay.py + scripts/warhammer_com/
playwright-stealth>=1.0.6  # scripts/warhammer_com/ stealth evasions for AWS WAF

# ─── Quality & dedup ──────────────────────────────────────────
open-clip-torch>=2.24      # CLIP quality scoring
imagehash>=4.3             # perceptual hash dedup

# ─── Inference server (optional, for FastAPI refactor) ────────
fastapi>=0.110
uvicorn>=0.29
python-multipart>=0.0.9    # FastAPI multipart form uploads
pydantic>=2.7              # request/response schemas

# ─── photoanalyzer library (src/photoanalyzer) ────────────────
# These are library-level deps the package will use.
faiss-cpu>=1.8             # ANN index for retrieval gallery
scikit-learn>=1.4          # linear probe Tier 2 classifier

# ─── Test infra ───────────────────────────────────────────────
pytest>=8.0
pytest-cov>=5.0
